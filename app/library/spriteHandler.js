'use strict';
class SpriteHandler {
    constructor() {
        this.sprites = new Map();
    }

    add(key, item){
        return this.sprite.set(key, item)
    }
    
    get(key){
        return this.sprite.get(key);
    }
}